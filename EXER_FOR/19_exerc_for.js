// Crie uma função que calcule a raiz quadrada de um número e retorne um número inteiro como resultado

function squareRootOfNumber(n){
    return parseInt(Math.sqrt(n))
}

console.log(`A raiz quadrada de ${4} é : ${squareRootOfNumber(4)}`);